// Creator Profiles evolve the existing persona identity model. Keep both API
// names live so formulas, batches, and older clients remain compatible.
export { GET, POST } from "@/app/api/personas/route";
